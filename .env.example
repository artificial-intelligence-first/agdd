# AGDD HTTP API configuration
# Copy this file to `.env` and adjust values for your deployment.

# Core server settings
AGDD_API_HOST=0.0.0.0
AGDD_API_PORT=8000
AGDD_API_PREFIX=/api/v1
AGDD_API_DEBUG=false

# Authentication
# Set to a strong secret to require Authorization or x-api-key headers.
AGDD_API_KEY=

# Cross-Origin Resource Sharing (CORS)
# Provide a JSON array of allowed origins, e.g. ["https://app.example.com"].
AGDD_CORS_ORIGINS=["*"]
AGDD_CORS_ALLOW_CREDENTIALS=false

# Observability
# Directory where `.runs/agents/<RUN_ID>` artifacts are stored.
AGDD_RUNS_BASE_DIR=.runs/agents

# Rate limiting
# Maximum requests per second per client. Leave empty to disable.
AGDD_RATE_LIMIT_QPS=
# Redis connection string for distributed rate limiting (e.g. redis://localhost:6379/0).
AGDD_REDIS_URL=

# GitHub integration
# Shared webhook secret used for HMAC verification.
AGDD_GITHUB_WEBHOOK_SECRET=
# Token with permission to post comments (Fine-grained PAT or GitHub App installation token).
AGDD_GITHUB_TOKEN=

# Additional environment variables can be added below as needed.
